name: Build APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build with Buildozer Action
      uses: PaulUniversal/buildozer-action@v1
      with:
        command: buildozer android debug
